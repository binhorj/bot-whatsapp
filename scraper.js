async function extrairDadosProduto(url) { const puppeteer = require('puppeteer-extra'); const StealthPlugin = require('puppeteer-extra-plugin-stealth'); puppeteer.use(StealthPlugin()); const browser = await puppeteer.launch({ headless: true }); const page = await browser.newPage(); await page.setUserAgent('Mozilla/5.0'); try { await page.goto(url, { waitUntil: 'domcontentloaded', timeout: 30000 }); const urlFinal = page.url(); let dados = await page.evaluate(function() { var nome = document.querySelector('meta[property="og:title"]') ? document.querySelector('meta[property="og:title"]').content : (document.querySelector('h1') ? document.querySelector('h1').innerText.trim() : null); var imagem = document.querySelector('meta[property="og:image"]') ? document.querySelector('meta[property="og:image"]').content : null; var preco = document.querySelector('meta[property="product:price:amount"]') ? document.querySelector('meta[property="product:price:amount"]').content : (document.querySelector('[class*="price"]') ? document.querySelector('[class*="price"]').innerText.trim() : null); return { nome: nome, preco: preco, imagem: imagem }; }); await browser.close(); if (!dados || !dados.nome) return null; return { nome: dados.nome, preco: dados.preco || 'Consulte o preco', imagem: dados.imagem || null, link: urlFinal }; } catch(e) { await browser.close(); console.error(e.message); return null; } } module.exports = { extrairDadosProduto: extrairDadosProduto };